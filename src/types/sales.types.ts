export interface SalesData {
  productId: string;
  customerName: string;
  quantity: number;
  totalAmount: number;
  price: number;
  tax: number; //if applicable
  discount: number; //if applicable
}
